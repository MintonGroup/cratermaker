name: deploy_cratermaker

on:
  workflow_dispatch:
  release:
    types:
      - published

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v6

      - name: Get history and tags for SCM versioning to work
        run: |
          git fetch --prune --unshallow
          git fetch --depth=1 origin +refs/tags/*:refs/tags/*

      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Set version
        shell: bash
        run: |
          python buildscripts/set_version.py

      - name: Install openblas/openssl (Linux host only; manylinux builds use container deps)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev libopenblas-dev

      - name:  Install OpenSSL OpenBLAS via Homebrew (macOS)
        if: runner.os == 'macOS'
        run: brew install openssl@3 openblas

      - name: Install OpenBLAS via vcpkg (Windows)
        if: runner.os == 'Windows'
        run: |
          vcpkg install openblas intel-mkl --triplet x64-windows
          echo "VCPKG_ROOT=C:\vcpkg" >> $env:GITHUB_ENV
          echo "OPENBLAS_LIB=C:\vcpkg\installed\x64-windows\lib" >> $env:GITHUB_ENV

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist --find-interpreter
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: auto
          before-script-linux: |
            set -eux
            # maturin builds Linux wheels in a manylinux Docker container; install deps *inside* that container.
            if command -v yum >/dev/null 2>&1; then
              yum -y install epel-release
              yum repolist
              yum -y install pkgconfig openblas-devel openssl11-devel
            elif command -v dnf >/dev/null 2>&1; then
              dnf -y install pkgconf-pkg-config flexiblas-devel openssl11-devel
            elif command -v apt-get >/dev/null 2>&1; then
              apt-get update
              apt-get install -y pkg-config libssl1.1-dev libopenblas-dev
            else
              echo "No supported package manager found in manylinux container" >&2
              exit 1
            fi

      - uses: actions/upload-artifact@v6
        with:
          name: cibw-wheels-${{ matrix.os }}
          path: dist/*.whl
          retention-days: 1

  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Get history and tags for SCM versioning to work
        run: |
          git fetch --prune --unshallow
          git fetch --depth=1 origin +refs/tags/*:refs/tags/*

      - uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Set version
        shell: bash
        run: |
          python buildscripts/set_version.py

      - name: Build sdist
        run: pipx run build --sdist

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-sdist
          path: dist/*.tar.gz              
          retention-days: 1 

  upload_pypi:
    needs: [build_sdist, build_wheels]
    runs-on: ubuntu-latest
    environment: 
      name: pypi
      url: https://pypi.org/p/cratermaker
    permissions:
      id-token: write
    if: github.event_name == 'release' && github.event.action == 'published'
    steps:
      - uses: actions/download-artifact@v7
        with:
          pattern: cibw-*
          path: dist
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true
